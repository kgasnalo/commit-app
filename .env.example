# .env.example
# Copy this file to .env and fill in your values
# See docs/SETUP.md for detailed instructions

# === REQUIRED ===
# These variables are mandatory. The app will crash if missing.

# Supabase configuration (from supabase.com dashboard)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe configuration (from stripe.com dashboard)
# Use pk_test_* for development, pk_live_* for production
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-key-here

# === OPTIONAL ===
# These variables are optional. Some features may be disabled if missing.

# Google Books API (for book search feature)
# Get from: https://console.developers.google.com/
EXPO_PUBLIC_GOOGLE_API_KEY=your-google-api-key

# === SERVER-SIDE ONLY (DO NOT USE IN CLIENT) ===
# The following keys should NEVER be included in client-side code.
# They should only be used in backend services (Supabase Edge Functions, etc.)
#
# STRIPE_SECRET_KEY=sk_test_xxx (use in Edge Functions only)
# SUPABASE_SERVICE_ROLE_KEY=xxx (use in Edge Functions only)
